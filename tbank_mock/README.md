# T-Bank mock

Local T-Bank Internet Acquiring mock with a hosted card form, two-stage
payments, SQLite persistence, and signed webhooks.

Run locally with:

```bash
uv run uvicorn tbank_mock.app:app --reload
```
